export class NonceManager {
    constructor() {
        this.lastNonce = 0;
    }
    getNonce() {
        let nonce = Date.now();
        if (nonce <= this.lastNonce) {
            nonce = ++this.lastNonce;
        }
        else {
            this.lastNonce = nonce;
        }
        return nonce;
    }
}
//# sourceMappingURL=nonce.util.js.map