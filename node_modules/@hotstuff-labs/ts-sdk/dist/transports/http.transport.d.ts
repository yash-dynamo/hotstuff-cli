import { TransportsTypes as TT } from '../types';
export declare class HttpTransport {
    isTestnet: boolean;
    private timeout;
    private server;
    private fetchOptions;
    private onRequest?;
    private onResponse?;
    constructor(options?: TT.IHttpTransportOptions);
    request<T>(endpoint: 'info' | 'exchange' | 'explorer', payload: unknown, signal?: AbortSignal, method?: 'GET' | 'POST'): Promise<T>;
}
//# sourceMappingURL=http.transport.d.ts.map