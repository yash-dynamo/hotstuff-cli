import { TransportsTypes as TT } from '../types';
export declare class WebSocketTransport {
    isTestnet: boolean;
    private timeout;
    private ws;
    private reconnectAttempts;
    private maxReconnectAttempts;
    private reconnectDelay;
    private server;
    private keepAlive?;
    private keepAliveInterval?;
    private messageQueue;
    private messageIdCounter;
    private subscriptions;
    private subscriptionCallbacks;
    private connectionPromise;
    constructor(options?: TT.IWebsocketTransportOptions);
    private ensureConnected;
    private cleanup;
    private startKeepAlive;
    private handleIncomingMessage;
    private handleJSONRPCResponse;
    private handleJSONRPCNotification;
    private sendJSONRPCMessage;
    private formatSubscriptionParams;
    private subscribeToChannels;
    private unsubscribeFromChannels;
    isConnected(): boolean;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    ping(): Promise<TT.PongResult>;
    private createAbortError;
    private normalizeRequestResult;
    request<T>(type: 'info' | 'exchange' | 'explorer', payload: unknown, signal?: AbortSignal): Promise<T>;
    subscribe<T>(channel: string, payload: unknown, listener: (data: CustomEvent<T>) => void): Promise<T>;
    unsubscribe(subscriptionId: string): Promise<void>;
    getSubscriptions(): TT.ISubscription[];
}
//# sourceMappingURL=websocket.transport.d.ts.map